main {

  appName = "PiJvmPoc"
  appName = ${?APP_NAME}

  authString = ${TOKEN}
  
  baseUrl = ${BASE_URL}

}

akka {

  # Options: OFF, ERROR, WARNING, INFO, DEBUG
  loglevel = ${?AKKA_LOG_LEVEL}
  loglevel = "INFO"

  actor {

    serializers {
    }
    serialization-bindings {
    }
  }

}
